const ROOT = "/";

const LANDING = "/";
const HOME = "/home";
const CALENDAR = "/calendar";
const AREA = "/area";
const SETTING = "/setting";

export const PAGE_URI = {
  HOME,
  CALENDAR,
  AREA,
  SETTING,
  LANDING,
  ROOT,
};
